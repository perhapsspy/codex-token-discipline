# AGENTS.md

- This repo publishes the `codex-token-discipline` skill.
- Keep task context with `$project-context`.
- The shipped skill contract lives in `skills/codex-token-discipline/SKILL.md`.
- Keep `SKILL.md` as the English base skill and `SKILL.ko.md` as a frontmatter-free Korean companion.
- Use Korean by default except for files that are explicitly English-facing.

## Boundaries

- README files introduce, install, and route to the skill.
- `docs/skill-direction.md` owns repo-local evolution criteria.
- Reference docs keep current trusted background, not runtime procedure duplicates.
- `skills/codex-token-discipline/` owns the installable runtime contract and bundled scripts.
